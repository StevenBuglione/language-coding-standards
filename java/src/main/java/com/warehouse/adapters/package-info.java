/**
 * Adapters layer: in-memory implementations of the application ports, for tests and demos. May
 * depend on the application and domain layers; nothing may depend on this layer (enforced by
 * ArchitectureTest).
 */
@NullMarked
package com.warehouse.adapters;

import org.jspecify.annotations.NullMarked;
