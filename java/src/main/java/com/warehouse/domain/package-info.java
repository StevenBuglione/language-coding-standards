/**
 * Pure domain model of the canonical warehouse-order domain (CONTRACTS.md §2): value objects, the
 * Order state machine, and typed domain errors. Depends on nothing outside the JDK.
 */
@NullMarked
package com.warehouse.domain;

import org.jspecify.annotations.NullMarked;
