# Standalone fixture workspace: excluded from the template's workspace by
# construction (root Cargo.toml exclude + this [workspace] table), std-only
# so every probe runs --offline.
[workspace]

[package]
name = "bad-too_complex"
version = "0.0.0"
edition = "2024"
