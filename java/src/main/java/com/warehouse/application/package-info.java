/**
 * Application layer of the canonical warehouse domain: outbound ports and the PlaceOrderUseCase
 * orchestration. May depend on the domain layer, never on adapters (enforced by ArchitectureTest).
 */
@NullMarked
package com.warehouse.application;

import org.jspecify.annotations.NullMarked;
