# Build outputs
target/

# Workspace-local tool caches (CONTRACTS.md §4): the Maven local repository
# and the Maven wrapper distribution cache stay inside the mounted workspace
# so they never leak outside it and never enter image layers.
.m2repo/
.m2wrapper/

# Coverage and mutation reports generated by the coverage/mutation tiers
site/
pit-reports/
